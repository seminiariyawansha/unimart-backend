-- Category seeded manually in an earlier run (id 6, 'Textbooks').
-- No inserts needed here for now — completed-order seeding happens later
-- in V3, once real listing/user IDs exist from Postman registration.
SELECT 1;